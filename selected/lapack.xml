<tool name="lapack" version="${LAPACK_VER}">
  <client>
    <environment name="LAPACK_BASE" default="${LAPACK_PREFIX}"/>
    <environment name="LIBDIR" default="$$LAPACK_BASE/lib"/>
  </client>
</tool>
