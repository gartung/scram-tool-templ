<tool name="vdt" version="${VDT_VER}">
  <lib name="vdt"/>
  <use name="vdt_headers"/>
  <client>
    <environment name="VDT_BASE" default="${VDT_PREFIX}"/>
    <environment name="LIBDIR" default="$$VDT_BASE/lib"/>
  </client>
  <flags SKIP_TOOL_SYMLINKS="1"/>
</tool>
